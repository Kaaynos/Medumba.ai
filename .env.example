# Copy to .env.local to point a local dev server at a Supabase project
# other than production (see src/config/supabase.js). Leave unset to use
# production, which is also what happens with no .env.local at all.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
